// TODO: change these dummy colours to the final colours!

import 'package:flutter/material.dart';

// DUMMY COLOURS
const selectedIconColour = Colors.red;
const unselectedIconColour = Colors.green;
const navBarColour = Colors.yellow;

// COLOURS FROM PROTOTYPE DOC
const lightColour = Color(0xFFF5F1E0);
const darkColour = Color(0xFF302721);

const accent = Color(0xFF83B356);
const mutedAccent = Color(0xFFBCE0A4);

const secondaryAccent = Color(0xFF9A7749);
const secondaryMutedAccent = Color(0xFFB79466);

const darkHighlight = Color(0xFF49837F);
const lightHighlight = Color(0xFF84CBCA);
